package com.bayneno.backen_manage_property.enums;

public enum EPropertyTypeGroupLog {
  N(1),
  B(2),
  A(3),
  L(4),
  T(5)
  ;

  int index;

  EPropertyTypeGroupLog(int index) {
    this.index = index;
  }
}
